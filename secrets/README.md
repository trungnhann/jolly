## Local Secrets

Thư mục này chứa secrets cho `docker compose` (local dev).

Tạo các file sau (KHÔNG commit):

- `secrets/postgres_password`
- `secrets/minio_root_user`
- `secrets/minio_root_password`

Bạn có thể copy từ các file `.sample` và thay giá trị.

